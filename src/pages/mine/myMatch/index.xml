<template name="feed-detail-invite">
	<navigator url="../../yaoyue/detail/index?id={{uid}}" class="feed-match-card">
		<view class="feed-detail-left">
			<view class="user-icon-wrapper">
				<image mode="aspectFill" class="user-icon" src="{{avatar}}"/>
				<view wx:if="{{verify_status == 2}}" class="vip">V</view>
			</view>
		</view>
		<view class="feed-detail-right">
			<view class="header">
				<view class="wrapper">
					<text class="user-name">{{nickname}}</text>
					<text class="user-company">{{company}}</text>
					<text class="user-position">{{position}}</text>
				</view>
			</view>
			<view class="feed-content">
				<text>{{content}}</text>
			</view>
			<view class="time">
				<text>{{created_at}}</text>
			</view>
		</view>
		<view wx:if="{{tag_identity_name !== ''}}" class="reward-item">
				<text>{{tag_identity_name}}</text>
		</view>
	</navigator>
</template>

<view>
	<block wx:if="{{props.match.myMatchList.length > 0}}"></block>
	<block wx:else>
		<view class="no-data">
			<text>您还没发布需求</text>
		</view>
	</block>
	<block wx:for="{{props.match.myMatchList}}"  wx:key="id">
		<template is="feed-detail-invite" data="{{...item, index: index}}" />
	</block>
</view>
